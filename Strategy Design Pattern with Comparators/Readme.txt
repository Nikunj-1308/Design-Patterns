With implmentation of Car with Strategy Design Pattern, created three comparator classes for
the Car class
– PriceComparator<Car>,  YearComparator<Car> and MileageComparator<Car>
– Implemented an extra comparator class ParetoComparator<Car>, which performs the Pareto comparison.